# Cloudflare Environment Variables
# Disse skal sættes i Cloudflare Pages UI under Settings → Environment variables

# Session token salt (generer random string, mindst 32 tegn)
# Eksempel: openssl rand -base64 32
SESSION_SECRET=generer_random_her

# IP hashing salt for rate limiting (generer random string, mindst 32 tegn)
# Eksempel: openssl rand -base64 32
IP_HASH_SALT=generer_random_her

# Environment marker (sættes automatisk af Cloudflare)
ENVIRONMENT=production
